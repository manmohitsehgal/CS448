create table Show(title varchar(50), hname varchar(50), attendance integer, artist varchar(50));

create table Hall(hname varchar(50), cname varchar(50), capacity integer);

create table City(cname varchar(50), country varchar(20), state varchar(20));


